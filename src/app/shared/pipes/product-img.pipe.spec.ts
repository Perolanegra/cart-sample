import { ProductImgPipe } from './product-img.pipe';

describe('ProductImgPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductImgPipe();
    expect(pipe).toBeTruthy();
  });
});
