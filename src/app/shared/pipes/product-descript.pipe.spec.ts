import { ProductDescriptPipe } from './product-descript.pipe';

describe('ProductDescriptPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductDescriptPipe();
    expect(pipe).toBeTruthy();
  });
});
